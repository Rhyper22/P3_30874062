<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css'/>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <title>Categorias</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">Navbar</a>
          <button class="navbar-toggler" type="button">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <a class="nav-link active" aria-current="page" href="/about">¿Quién Soy?</a>
                <a class="nav-link active" aria-current="page" href="/">Volver a la página principal</a>
              <li class="nav-item">
              </li>
            </form>
          </div>
        </div>
      </nav>
      
      <table class="table table-bordered table-striped text-center mt-4">
        <thead>
          <tr>
            <th>ID</th>
            <th>Categorias Existentes</th>
            <th>Nuevas Categorias</th>
          </tr>
        </thead>
        <tbody>
          <% categorias.forEach(categoria => { %>
            <tr>
              <td><%= categoria.id %></td>
              <td></td>
              <td><%= categoria.Nombre %></td>
              <td><a class="btn btn-primary" href="/edit-categorias/<%= categoria.id %>">Editar</a>
                <a class="btn btn-danger" href="/delete-categorias/<%= categoria.id %>">Borrar</a>
            </tr>
            <% }); %>
        </tbody>
        </table>
        <a href="/insert-categoria" class="button"><i class='bx bxs-plus-circle bx-md'></i></a>
</body>
</html>